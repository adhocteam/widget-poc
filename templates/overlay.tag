<plan-overlay>
  <div class="overlayDetails">
    <ul>
      <overlay-line count="{opts.doctors}" section="doctors" label="Doctors"/>
      <overlay-line count="{opts.scrips}" section="scrips" label="Prescriptions"/>
      <overlay-line count="{opts.facilities}" section="facilities" label="Facilities"/>
    </ul>
    <a href="javascript:;" data-modal=true>View All / Edit</a>
  </div>
</plan-overlay>
